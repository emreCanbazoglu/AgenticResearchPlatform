# Quickstart: 001 Research Platform Orchestration

## Goal

Run a deterministic local batch and verify reproducible outputs.

## Planned Flow

1. Configure campaign objective and optimizer policy.
2. Generate jobs from planner.
3. Run worker pool in local multiprocess mode.
4. Persist metrics and lineage metadata.
5. Re-run with identical snapshot and compare outputs.
